<%
do_disable_authorization = true

content = File.read(File.expand_path("jobs/mongodb-server/templates/config/mongod.conf.erb", ENV["RELEASE_DIR"]))
-%>
<%= ERB.new(content, safe_level = nil, trim_mode = "-").result(binding) %>
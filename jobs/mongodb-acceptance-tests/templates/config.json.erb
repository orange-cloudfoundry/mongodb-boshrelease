{
  "timeout_scale":                    <%= p('acceptance_tests.timeout_scale') %>,
  "mongo_host":                       <%= p('acceptance_tests.mongo_host') %>,
  "mongo_port":                       <%= p('acceptance_tests.mongo_port') %>
}